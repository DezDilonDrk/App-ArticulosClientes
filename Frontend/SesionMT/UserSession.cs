namespace SesionMT
{
    public static class UserSession
    {
        private static HttpClient client = new HttpClient();
        public static string token = ""; //Sustituir lo del front por esto, para usarlo aquí y en Test
    }
}
